format_version = 1
name = "Kill Lead"
description = "Free for all. First player to lead the field by the target wins."
identity = ["rules.teams", "rules.victory.type"]

[rules]
teams = false
suicide_penalty = "kill_no_negative"

[rules.victory]
type = "kill_lead"
target = 3
