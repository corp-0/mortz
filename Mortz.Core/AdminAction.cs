namespace Mortz.Core;

/// <summary>Stable action ids included in signed privileged lobby mutations.</summary>
public static class AdminAction
{
    public const byte SET_LOBBY_RULES = 1;
    public const byte SET_LOBBY_MAP = 2;
}
