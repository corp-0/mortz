format_version = 1
name = "Kill Lead"
description = "Free for all. First player to lead the field by the target wins."
identity = ["rules.teams", "rules.score", "rules.winner", "rules.evaluation", "rules.replay", "rules.end_condition.type"]

[rules]
score = "kills"
winner = "highest_score"
evaluation = "elimination"
replay = "decisive_elimination"
teams = false
suicide_penalty = "kill_no_negative"

[rules.end_condition]
type = "score_lead"
target = 3
