namespace Mortz.Core.Match.Configuration;

public enum ScoreMetric { KILLS, DEATHS }

public enum WinnerRule { HIGHEST_SCORE, LOWEST_SCORE }

public enum EvaluationTiming { ELIMINATION, TICK }

public enum ReplayRule { DECISIVE_ELIMINATION, NONE }
