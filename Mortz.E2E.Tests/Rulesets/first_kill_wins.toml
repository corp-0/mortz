# Deathmatch with target=1, so the match ends after one shot, not five.
[rules]
score = "kills"
winner = "highest_score"
evaluation = "elimination"
replay = "decisive_elimination"
teams = false
suicide_penalty = "kill_no_negative"

[rules.end_condition]
type = "score_target"
target = 1
